// Code generated by Wire. DO NOT EDIT.

//go:generate go run github.com/google/wire/cmd/wire
//go:build !wireinject
// +build !wireinject

package main

import (
	"github.com/cloudwego/hertz/pkg/app/server"
	"github.com/go-hasaki/hasaki-layout-advanced/internal/handler"
	"github.com/go-hasaki/hasaki-layout-advanced/internal/middleware"
	"github.com/go-hasaki/hasaki-layout-advanced/internal/repository"
	server2 "github.com/go-hasaki/hasaki-layout-advanced/internal/server"
	"github.com/go-hasaki/hasaki-layout-advanced/internal/service"
	"github.com/go-hasaki/hasaki-layout-advanced/pkg/helper/sid"
	"github.com/go-hasaki/hasaki-layout-advanced/pkg/log"
	"github.com/google/wire"
	"github.com/spf13/viper"
)

// Injectors from wire.go:

func newApp(viperViper *viper.Viper, logger *log.Logger) (*server.Hertz, func(), error) {
	jwt := middleware.NewJwt(viperViper)
	handlerHandler := handler.NewHandler(logger)
	sidSid := sid.NewSid()
	serviceService := service.NewService(logger, sidSid, jwt)
	db := repository.NewDB(viperViper)
	client := repository.NewRedis(viperViper)
	repositoryRepository := repository.NewRepository(db, client, logger)
	userRepository := repository.NewUserRepository(repositoryRepository)
	userService := service.NewUserService(serviceService, userRepository)
	userHandler := handler.NewUserHandler(handlerHandler, userService)
	hertz := server2.NewServer(viperViper, logger, jwt, userHandler)
	return hertz, func() {
	}, nil
}

// wire.go:

var ServerSet = wire.NewSet(server2.NewServer)

var SidSet = wire.NewSet(sid.NewSid)

var JwtSet = wire.NewSet(middleware.NewJwt)

var HandlerSet = wire.NewSet(handler.NewHandler, handler.NewUserHandler)

var ServiceSet = wire.NewSet(service.NewService, service.NewUserService)

var RepositorySet = wire.NewSet(repository.NewDB, repository.NewRedis, repository.NewRepository, repository.NewUserRepository)
